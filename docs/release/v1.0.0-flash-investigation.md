---
date: 2026-05-04
context: Validation device booted vanilla LineageOS first-boot wizard while preparing LinkedIn screenshots for v1.0.0
---

# v1.0.0 — flash investigation (2026-05-04)

## Symptom

Validation device (Galaxy Note II, t0lte/t03g) booted into the **stock LineageOS first-boot wizard**. No LETHE boot animation, no teal-on-black theme, no `ro.lethe.*` props — straight vanilla LOS branding. Screenshots for the LinkedIn post are blocked until a real LETHE build is on the device.

## Root cause (most likely)

The wrong zip was flashed. `Vanilla-cm14-t0lte.zip` (vanilla LineageOS reference, used to validate the install pipeline) is the **most recently modified file before the device's last boot**:

| File | Size | mtime |
|---|---|---|
| `Lethe-1.0.0-t0lte.zip` | 320 MB | 2026-05-02 18:39 |
| `Vanilla-cm14-t0lte.zip` | 271 MB | 2026-05-02 16:21 |
| `Lethe-1.0.0-t03g.zip` | 5 KB (!) | 2026-03-29 14:47 |
| `Lethe-1.1.0-t0lte.zip` | 5.2 MB (!) | 2026-04-14 18:51 |

Both zips live next to each other in `/home/mia/Osmosis-downloads/lethe-builds/`. Naming is similar enough that a misclick / wrong path is plausible.

## Build artifact audit

### `Lethe-1.0.0-t0lte.zip` — the real OTA, ✅ usable

Real signed block-OTA. 320 MB, `system.new.dat` is 610 MB. Confirmed LETHE overlay applied:

```
ro.lethe=true
ro.lethe.version=1.0.0
ro.build.display.id=LETHE-1.0.0
ro.lineage.display.version=LETHE-1.0.0
ro.cm.version=14.1-20260502-UNOFFICIAL-t0lte
persist.lethe.burner.enabled=true       # the 6f2d06e fix landed
persist.lethe.tor.enabled=true
persist.lethe.ipfs.enabled=true
persist.lethe.{bg_loc,body_sens,near_dev,clip_iso,...}
```

Updater-script asserts the t0lte family (12 codenames including SGH-T889, GT-N7105). **This is the zip we need on the device.**

### `Lethe-1.0.0-t03g.zip` — stub, ❌ unusable

Only 5 KB. Contains 7 loose overlay config files (`privacy-defaults.conf`, `hosts`, `firewall-rules.conf`, etc.) plus an `updater-script`. **No `system.new.dat`, no boot.img, no signing block.** This is an artifact from the "silent-shipping nothing" era documented in [v1.0.0-build-notes.md:11-16](v1.0.0-build-notes.md#L11) — the `add_to_system` / `PRODUCT_COPY_FILES` fix in 73d507d landed *after* this was built (March 29). It cannot be flashed and must not be published as the t03g release.

### `Lethe-1.1.0-t0lte.zip` — too small to be a real OTA, ❌ suspect

5.2 MB. A full LineageOS OTA is 250–350 MB. Either an empty/broken build artifact, a leftover from an aborted run, or an incremental (but the meta.json doesn't mark it as such). Should not be published until verified.

### `Lethe-1.0.0-t0lte.zip.sha256` and `.sig` — stale, ⚠️ regenerate

Signature/checksum files are dated **2026-05-01 14:54**, but the zip was rebuilt **2026-05-02 18:39**. They were generated against an earlier build of the same filename:

- recorded sha256: `e91654dec606ca9a54387b937fcc13369f82655d11165bdd9c23ba3492196273`
- actual sha256:   `25a32a937f79f96c29d0617338e1f2e8d57bed2fa521cf451a10a93cb83f62e5`

`.sig` is 64 bytes (Ed25519). Both must be regenerated against the current zip via `scripts/sign-build.sh` before the GitHub release goes up — anyone verifying will fail today.

### `Lethe-1.0.0-t0lte-meta.json` — describes a different build, ⚠️ regenerate

Generated 2026-03-29. Claims:

```
"launcher": "void",
"features": [..., "dead_mans_switch", "launcher", ...]
"installed": "2026-03-29"
"notes": "Updated with Void launcher, ..."
```

The v1.0.0 release explicitly de-scopes Void launcher, dead-man's-switch, and the agent (per [LINKEDIN-POST.md:17](LINKEDIN-POST.md#L17) and [v1.0.0-build-notes.md:87-100](v1.0.0-build-notes.md#L87)). Feeding this meta.json to anyone reading it ships a contradiction. Must be regenerated against the actual May-2 build with the trimmed feature list.

### Pre-rendered theme assets — ✅ usable

`/home/mia/Osmosis-downloads/t03g/` has fresh PNG renders from the theme pipeline:

- `lethe-lockscreen.png` (215 KB)
- `lethe-wallpaper.png` (266 KB)
- `lethe-boot-logo.png` (792 KB)
- `bootanimation.zip` (2.1 MB) — t03g version
- `lethe-bootlogo-t03g.zip`, `lethe-theme-t03g.zip`

These are usable as supplementary screenshots if the device flash takes too long, but the LinkedIn hero shot should be a photo of the actual device running LETHE, not a render.

## Flashing gotcha (if t03g/t0lte mismatch)

Build notes flag this: TWRP on the LTE unit reports `ro.product.device=t03g` but the OTA's updater-script asserts `t0lte`-family — sideload aborts. Strip-and-resign workaround at [v1.0.0-build-notes.md:62-77](v1.0.0-build-notes.md#L62-L77).

## Action items (in order)

1. **Reflash the device** with `Lethe-1.0.0-t0lte.zip`. If TWRP reports t03g, apply the strip-and-resign procedure first.
2. **Wait the full 25–30 min** of first-boot dexopt without declaring failure ([v1.0.0-build-notes.md:7](v1.0.0-build-notes.md#L7)).
3. **Take screenshots** (boot animation, lockscreen, About screen with `LETHE-1.0.0` build string) + a real-device photo for the LinkedIn hero.
4. **Regenerate `.sha256`, `.sig`, and `-meta.json`** against the current `Lethe-1.0.0-t0lte.zip` before publishing the GitHub release.
5. **Decide on t03g**: either build a real t03g OTA, or drop t03g from the v1.0 release and document that only t0lte ships in v1.0 (the LTE variant). The 5 KB stub must not appear on the release page.
6. **Investigate `Lethe-1.1.0-t0lte.zip` (5.2 MB)** — either it's a known incremental and the metadata should say so, or it's a broken artifact and should be removed.

## What this is *not*

This is not a recurrence of any of the 13 build/init bugs from the May 1-2 push. The OTA at `Lethe-1.0.0-t0lte.zip` actually contains LETHE overlays correctly. The failure mode here is purely **operational** — a stale checksum, a stub t03g zip from before the build fixes, and (most likely) the wrong file flashed during validation.

## Resolution (2026-05-04)

1. **Strip-and-resign + sideload completed.** Original `Lethe-1.0.0-t0lte.zip` is for the t0lte family (GT-N7105 etc.); installed TWRP on this LTE unit reports `device:t03g` (the gotcha at [build-notes:62](v1.0.0-build-notes.md#L62)). Followed the documented procedure: `unzip → sed -i '1d' updater-script → re-zip → signapk.jar -w` (used JDK8 to dodge Java 17 module access errors against `sun.security.x509`). Wiped /data via `adb shell twrp wipe data` to remove `packages.xml` so `otasigcheck.sh` exits 0. Sideloaded successfully — `Updater process ended with RC=0`, 660 MB written to /system, 90 second install, no SIGBUS retry needed. Resigned working file at `/tmp/lethe-resign/Lethe-1.0.0-t0lte-noassert.zip`. Reboot issued at 10:15:39; entering 25-30 min first-boot dexopt.

2. **`.sha256` and `.sig` regenerated** via `scripts/sign-build.sh t0lte`. Signed against the actual May-2 zip (`25a32a937f…`); openssl verify-roundtrip OK. Earlier stale May-1 sigs (`e91654de…`) overwritten.

3. **`Lethe-1.0.0-t0lte-meta.json` rewritten.** Dropped `"launcher": "void"` and the `dead_mans_switch` / `launcher` / `encryption` entries from the features list — those don't reflect what's actually in the zip's `system/build.prop` and they contradict the v1.0 LinkedIn announcement. Added `tor` (which *is* shipping in v1.0). Added a `deferred_to_v1_1` array so anyone reading the meta sees the same scope split the release notes use. Replaced misleading `"installed": "2026-03-29"` (March stub date) with `"built": "2026-05-02"` (real build date from `ro.cm.version`). Embedded the fresh sha256.

4. **t03g dropped from v1.0 release artifacts.** Decision: ship only `t0lte` for v1.0. Reasoning: the only thing we have for t03g is the 5 KB pre-fix stub from March 29, and the LinkedIn post already declares the release "Validated on one device so far" (the t0lte LTE). A real t03g build needs the cm-14.1 Docker pipeline + a separate validation pass on actual 3G hardware — both v1.0.x or v1.1 work. Manifest unchanged (t03g remains a listed device for future builds). Local stub artifacts renamed to `Lethe-1.0.0-t03g.zip.STUB-NOT-FOR-RELEASE` and `…-meta.json.STUB-NOT-FOR-RELEASE` so a glob upload to GitHub can't grab them.

## Still outstanding

- **`Lethe-1.1.0-t0lte.zip` (5.2 MB, 2026-04-14).** Way under full-OTA size, no incremental marker in its meta. Either a known incremental that needs metadata fixing, or a leftover broken artifact that should be removed. Not blocking v1.0 — but worth resolving before v1.1 release.

## NEW (2026-05-04, post-boot validation): Tor + iptables not running

**On-device boot validation revealed a release-blocking gap.** The OTA installs and the property layer is intact — every `persist.lethe.*` is set, init services register, configs land in `/system/extras/lethe/` — but the network-hardening features that the LinkedIn post centers on are **not actually running**:

```
init.svc.lethe_tor       = restarting (crash loop)
avc: denied { execute_no_trans } for path="/system/bin/tor"
                          scontext=u:r:init:s0
                          tcontext=u:object_r:system_file:s0
init.svc.lethe_tor_rules = stopped (oneshot — could not write iptables either)
```

Root cause: the build's compiled `file_contexts.bin` and loaded sepolicy contain **no LETHE labels at all**:

```
$ unzip -p Lethe-1.0.0-t0lte.zip file_contexts.bin | strings | grep -iE 'lethe|tor'
(empty)
$ adb shell 'cat /sys/fs/selinux/policy | strings | grep ^lethe'
(empty)
```

The `tor` binary on disk is labeled the default `u:object_r:system_file:s0` instead of `u:object_r:lethe_exec:s0`, so init's `execute_no_trans` is denied (correctly — without the policy rule from `sepolicy/lethe.te`, init has no permission to execute it).

The repo's source policy is **correct**: [sepolicy/lethe.te](../../sepolicy/lethe.te) defines the `lethe` domain and `lethe_exec` type, [sepolicy/file_contexts](../../sepolicy/file_contexts) labels `/system/bin/tor` and the iptables script as `lethe_exec`, and `apply-overlays.sh` step 12 calls `install-sepolicy.sh` to copy them into the LineageOS source tree. So the failure is at the *build-system* level: install-sepolicy.sh's target dirs (`device/lineage/sepolicy/{common,vendor}`) likely don't exist on cm-14.1 (they're cm-12-style paths), and the script's `if [ -d "$target" ]; then` falls through silently to a fallback that the cm-14.1 `BOARD_SEPOLICY_DIRS` macro doesn't pick up. **Same family as bug #1 from the build notes** (silent-ship-nothing) — copy succeeds, build skips.

This is the same SELinux-execute issue that build-notes #2 (`capabilities` keyword) was trying to dodge by setting `seclabel u:r:init:s0`. The seclabel workaround keeps the service in the init context, but init *still* can't `execute_no_trans` a `system_file`-labeled binary into itself — it needs the `lethe_exec` label loaded plus an `allow init lethe_exec:file execute_no_trans` rule, both of which are present in the source policy and absent in the shipped policy.

**Impact on the LinkedIn post**: the post claims *"All traffic forced through Tor by iptables, UDP dropped."* As shipped, **neither Tor nor the iptables rules execute**. Networking on the device works in the clear. This claim must either be backed by a real fix or pulled from the post before publication.

What does still work on the as-shipped build (verified on-device):

- `ro.build.display.id=LETHE-1.0.0`, `ro.lethe=true/version=1.0.0`
- All `persist.lethe.*` props (sensor permissions, MAC randomization config, clipboard isolation, mic/cam indicators, PIN scramble, burner-mode flag)
- LETHE init services registered (burner, settings, tor, tor_rules — burner and settings reach `stopped` cleanly)
- LETHE bootanimation.zip on `/system/media/`, configs in `/system/extras/lethe/`
- Tracker hosts file, debloat, no Google services — all LineageOS-side and intact

What does *not* work as-shipped:

- Tor transparent proxy (SELinux blocks execve)
- iptables transparent-proxy / UDP-drop rules (same SELinux family of denials, plus init's iptables write requires the rules to actually fire from a labeled context)
- Anything else gated on a `lethe_exec`-labeled binary executing — agent and ipfs are out of scope for v1.0 anyway, but any v1.1 service inherits this same build-system gap

## Release decision needed

Three viable paths:

1. **Drop the Tor / iptables claims from the LinkedIn post.** Position v1.0 as "burner mode + tracker blocking + hardened DNS + sensor permissions" and move "force-through-Tor" to v1.1 alongside the agent and IPFS OTA. Honest, ships today, defers the network-hardening narrative.
2. **Fix sepolicy install path, rebuild, re-flash, re-validate.** Likely fix: harden `install-sepolicy.sh` to land at `device/samsung/t0lte/sepolicy/` (device-tree-local) or `vendor/lineage/sepolicy/legacy-um/` (cm-14.1 location) and confirm the path is in `BOARD_SEPOLICY_DIRS` for the t0lte target. Rebuild → strip-and-resign for t03g-TWRP → sideload → 25-30 min dexopt → re-screenshot. Several hours for a clean v1.0.
3. **Runtime workaround for the validation device only.** `setenforce 0` (permissive) lets Tor + iptables run, screenshots become possible, but this is a security regression you absolutely do not ship — only useful if we want to capture proof-of-concept screenshots while the proper fix is in flight.

I have a strong opinion: **path 1 for the v1.0 announcement today, path 2 as v1.0.1**. The post's other claims are defensible as-shipped; pulling Tor brings the post in line with what the device actually does, and a v1.0.1 patch with the sepolicy fix is a small, focused follow-up rather than a delayed launch.

## Update — the bug was *which zip*, not the build pipeline

User chose path 2 ("rebuild with sepolicy"). Before kicking off a fresh build, I dug into the LineageOS source tree at `/home/mia/android/lineage` to understand the install-sepolicy.sh path bug — and found the actual story is different and simpler.

**The May 1 LineageOS-output OTA at `out/target/product/t0lte/lineage-14.1-20260501-UNOFFICIAL-t0lte.zip` is correct.** It was built inside the cm-14.1 Docker container (paths in intermediates show `/lineage/out/...`). Its `file_contexts.bin` *does* include LETHE labels (`/system/bin/tor → lethe_exec`, etc.), and `installed-files.txt` lists all 13 LETHE binaries/scripts plus `/system/bin/tor`, `/system/etc/tor/torrc`, and `/system/etc/init/init.lethe-{burner,settings,tor}.rc`. Build's policy.conf compiled the lethe domain cleanly.

**The May 2 zip in `Osmosis-downloads/lethe-builds/Lethe-1.0.0-t0lte.zip`** was a *different* artifact:

- 320 MB vs 274 MB
- Different `boot.img` md5 (`fab65805…` vs `6d4609ec…`)
- Smaller `file_contexts.bin` (91 KB / 1186 string lines, no LETHE) vs the LineageOS-built one (96 KB / 1246 lines, all LETHE labels present)

I don't yet know what produced the May 2 zip. It carries the LETHE props in `system/build.prop` so it wasn't pristine vanilla, but its sepolicy/file_contexts trail says it was assembled outside the LineageOS sepolicy build flow. Possibilities to investigate before v1.1: a script that re-zips a vanilla LineageOS OTA with `system.new.dat` from a LETHE build but reuses the vanilla `file_contexts.bin` from a different `out/` directory; a hand-edited zip; a build from a tree where `device/samsung/t0lte/selinux/` had been clobbered. Whichever, the file went into `Osmosis-downloads/lethe-builds/` and got grabbed for validation.

### Action taken (2026-05-04, after path-2 decision)

1. Archived the broken artifact: `Lethe-1.0.0-t0lte.zip{,.sha256,.sig} → *.MAY2-BROKEN-NO-SEPOLICY` (kept on disk for forensics; do not upload).
2. Promoted `lineage-14.1-20260501-UNOFFICIAL-t0lte.zip` from the LineageOS build tree to `Lethe-1.0.0-t0lte.zip` in the release dir.
3. Re-signed Ed25519 → new sha256 `98c9bf88a3141ecf321eacff1e572f3ac5255907ebdf4ee15818a6871abb7f49`. Verify-roundtrip OK.
4. Updated `Lethe-1.0.0-t0lte-meta.json` — fresh sha256, `built: 2026-05-01`, `cm_version: 14.1-20260501-…`, note explicitly mentions sepolicy was built into this zip.
5. Strip-and-resign for sideload (TWRP-on-this-LTE-reports-t03g gotcha, same procedure as before).
6. Sideloaded onto device. RC=0, 66 s install. Reboot at 11:01:42 — into the 25-30 min first-boot dexopt window. Verification on the next boot will be the screencap script:
   - `getprop ro.lethe / ro.lethe.version` → present
   - `getprop init.svc.lethe_tor` → expect `running` (was `restarting` before)
   - `logcat -d -s init | grep -i tor` → no `avc denied execute_no_trans` lines
   - `iptables -t nat -L OUTPUT` (via `adb root`) → expect Tor-redirect rules

### Pending items going into v1.1

- **`install-sepolicy.sh` still has cm-12-style target paths** (`device/lineage/sepolicy/{,common,vendor}`) that don't exist on cm-14.1. The current device tree at `device/samsung/t0lte/selinux/` was populated by some earlier process (mtimes April 14-17), and the May 1 build picked it up via `BOARD_SEPOLICY_DIRS += device/samsung/t0lte/selinux` in BoardCommonConfig.mk. That is a fragile state — a fresh `apply-overlays.sh` run would currently fall through to the `vendor/lethe/sepolicy` fallback (not in BOARD_SEPOLICY_DIRS) and leave the policy unbuilt. Fix in v1.0.1 / v1.1: pass codename to install-sepolicy.sh and target `device/<vendor>/<codename>/selinux/` for cm-14.1, with the lineage cm-12-style fallback only for older trees.
- **Find the May 2 build's origin.** Until we know what produced the broken artifact, the same mistake can re-occur on the next release. Most likely a stray script in `web/routes/lethe_build.py` or `lethe_ota.py` (which I noticed during the file search) — those should be audited.
- **Lethe-1.1.0-t0lte.zip** (5.2 MB) still suspect — same family of repackaging artifact, probably.
- **Kernel splash glitches**: on the t0lte unit, the LETHE pre-init framebuffer splash (`lethe-splash-rgb565.raw`, set via `lethe-bootlogo-t03g.zip`) renders with red pixel-line glitches before bootanimation.zip plays. Symptom is consistent with an RGB565 byte-order or stride mismatch in the raw generator — not a hardware/display issue, not introduced by the May-1 OTA. Cosmetic only; bootanimation phase looks clean. Fix in v1.0.1: re-render the splash with explicit `RGB565` (high byte first) byte order and verify against the t0lte 720×1280 panel before re-packaging the bootlogo zip. Workaround for v1.0 screenshots: skip splash, use bootanimation frames.

## NEWER (2026-05-04, after the May-4 rebuild): burner-wipe parent-dir bug

**The May-4 build still crash-loops zygote — different cause.** With the d82e555 props removed, the zygote-killer combo is gone. With sepolicy compiled in, Tor can finally execute. But once `lethe_burner_wipe` actually runs at `post-fs-data`, it does this:

```sh
rm -rf /data/app /data/data /data/user /data/user_de
```

Removing those four directories *as directories* leaves `installd` unable to recreate per-package data dirs at the next phase. `system_server` then crashes during `PackageManagerService` init, init kills `zygote` (because its child died), zygote restarts, system_server crashes again — for ~85 minutes (5163 s) until the device's battery couldn't sustain the loop.

Verified on the device: after the failed boot, mounted /data from TWRP and `ls /data/data` / `/data/user/0` / `/data/user_de/0` / `/data/app` all returned 0 entries — and the parent dirs themselves were empty (or had been recreated by some later init pass without their expected contents).

### Why May-2 didn't crash on this

The May-2 (no-sepolicy) artifact had `persist.lethe.burner.enabled=true` in its build.prop and shipped the same wipe script. It nonetheless reached the setup wizard. Most likely explanation: the `lethe_burner_wipe` service declares `seclabel u:r:init:s0`, which init resolves against the loaded sepolicy. Without a `lethe` domain or the `init` rules that the LETHE policy adds, init may have failed-soft on the seclabel and never started the service. With sepolicy compiled in (May-4), the seclabel resolves cleanly → service runs → wipe nukes the parent dirs. So the same script that's silent on May-2 is destructive on May-4.

The build notes' fix #5 (replace `settings put` with `rm -f settings_secure.xml`) was driven by exactly this kind of post-fs-data interaction; the parent-dir-vs-contents distinction is the same family of mistake — wholesale `rm -rf` of dirs init/installd expect to exist.

### Fix

Replace the `rm -rf /data/{app,data,user,user_de}` with a contents-only wipe that preserves the parent dirs:

```sh
for d in /data/app /data/data /data/user/0 /data/user_de/0; do
    [ -d "$d" ] && find "$d" -mindepth 1 -maxdepth 1 -exec rm -rf {} +
done
```

Applied in [scripts/runtime/lethe-burner-wipe.sh](../../scripts/runtime/lethe-burner-wipe.sh) (this commit). Also pushed to the validation device's `/system/bin/` for an immediate boot test before another full rebuild (TWRP `mount -o remount,rw /system && adb push`). The /data/data, /data/user_de/0, /data/app parent dirs were also re-created on the device with proper ownership + restorecon since the failed boot left them gone.

If this boot succeeds, the source fix needs to ride in a clean rebuild before any release artifact is uploaded — the OTA at `Lethe-1.0.0-t0lte.zip` still has the broken wipe script.

## NEWEST (2026-05-04, late afternoon): the sepolicy itself breaks boot

The wipe-script fix was correct but not the only bug. After applying the fix on-device and rebooting, zygote still crash-looped. Did three controlled boot tests on the May-4 build to bisect:

| Test | sepolicy | lethe init.rc | /data | Result |
|---|---|---|---|---|
| 1 | loaded | enabled (incl. fixed wipe) | empty (post-format) | crash-loop, splash never replaced |
| 2 | loaded | all renamed to `.disabled` | corrupt from prior boot | crash-loop |
| 3 | loaded | all renamed to `.disabled` | freshly Format-Data'd | crash-loop, drained battery off USB |

In test 3 the device should have booted essentially as vanilla LineageOS (no LETHE init services running, no LETHE wipe script, fresh /data). It didn't. The only LETHE component still active across all three tests is the **compiled SELinux policy** — the `lethe` domain, `lethe_exec` exec_type, and `init_daemon_domain(lethe)` macro expansion baked into the built `sepolicy` binary and `file_contexts.bin`.

`/proc/last_kmsg` from each failed boot showed init repeatedly killing zygote with SIGKILL (`Service 'zygote' (pid X) killed by signal 9` followed by init reaping audioserver/cameraserver/media/netd in the same group). That's init's standard reaction to a system_server child dying — system_server is what's actually crashing. No `avc:` lines, no `selinux denied` lines, no kernel panic, no OOM. Whatever the rule violation is, it manifests as a system_server death rather than a logged audit denial — possibly a `auditallow` that's silenced or a domain transition that fails closed before the audit subsystem logs it.

The May-2 (no-sepolicy) artifact reaches the LineageOS first-boot setup wizard cleanly, which is why we knew vanilla cm-14.1 + the LETHE prop set is bootable. The differentiating factor is exclusively the compiled policy.

### Decision (2026-05-04, ~16:00)

**Strip the lethe sepolicy from the v1.0 build and ship without Tor enforcement.** Rationale:

- Three controlled tests + one full-day investigation point at the policy as the breakage. Bisecting which rule (vs. the macro expansion vs. compilation order) is a multi-hour job and we are not within v1.0's timeline anymore.
- v1.0's other claims — burner mode (now with the parent-dir fix), tracker hosts, hardened DNS-over-TLS, sensor-permission defaults, no Google services — are independently verifiable and survive a rebuild without lethe.te.
- A no-sepolicy build matches the May-2 artifact's known-bootable behavior. Tor binary and torrc still ship under `/system/bin/tor` and `/system/etc/tor/`, but with no policy domain to transition into, init's `execute_no_trans` is denied, the Tor service stays in `restarting` state, and traffic is *not* in fact forced through Tor. The LinkedIn post must reflect this.

Concrete changes for the v1.0 strip:

1. Move `device/samsung/t0lte/selinux/lethe.te` → `lethe.te.disabled-in-v1.0` (preserve for v1.1 work).
2. Remove the 13 LETHE lines from `device/samsung/t0lte/selinux/file_contexts` (preserve untouched in `sepolicy/file_contexts` upstream).
3. Rebuild → sideload → boot validation → screenshots.
4. Edit `docs/release/LINKEDIN-POST.md`: pull the *"All traffic forced through Tor by iptables, UDP dropped"* sentence; either remove cleanly or replace with something honest about hardened DNS-over-TLS + tracker blocking + sensor lockdown.
5. v1.1 work: bisect lethe.te to find the offending rule (suspect: `init_daemon_domain(lethe)` macro expansion vs. cm-14.1 system_server class transitions, or the `set_prop(lethe, system_prop)` rule), restore Tor + iptables.

Filed as [GitHub issue #122](https://github.com/thdelmas/lethe/issues/122) for v1.1 with full reproduction. The investigation summary above is the authoritative record.

## NEWEST-NEWEST (2026-05-04, ~17:00): the no-sepolicy build also fails to boot

The hypothesis from the previous section was wrong, or at least incomplete. After:

1. Moving `device/samsung/t0lte/selinux/lethe.te` → `lethe.te.disabled-in-v1.0`.
2. Stripping the LETHE lines from `device/samsung/t0lte/selinux/file_contexts`.
3. Removing `system/sepolicy/domain.te:495`'s `-lethe` neverallow exception (which had been added when the lethe domain was introduced; checkpolicy refused to compile with a dangling type reference).
4. Updating the staged `lethe-staging/system/bin/lethe-burner-wipe.sh` with the parent-dir-preservation fix from the previous section.
5. Successfully rebuilding (13:31, sepolicy compile clean, file_contexts.bin verified empty of LETHE labels, all LETHE props intact).
6. Format-Data + sideload (one SIGBUS retry) + reboot at 16:23:46.

**The device crash-looped for 40 minutes and dropped off USB** — the exact same outcome as every May-4 boot before it. Even with no compiled sepolicy types, the build does not boot.

So the issue cuts deeper than the sepolicy alone. The May-2 archived artifact (`*.MAY2-BROKEN-NO-SEPOLICY`) reaches the setup wizard cleanly; the May-4 builds — regardless of sepolicy state, init.rc state, /data state — do not. We have not identified the differentiating factor.

Possible causes still on the table:

- **Source-tree drift between May-2 and May-4 builds.** The May-2 zip was built somewhere we haven't located (Docker volume, alternate tree); the May-4 builds come from `/home/mia/android/lineage`, which has accumulated patches since April. Some ramdisk/init.rc/Android.mk change since May-2 may have introduced this regression independent of the LETHE overlays.
- **Updated `lethe-staging/system/bin/lethe-burner-wipe.sh` running with the new logic** — even contents-only wipe of `/data/{app,data,user/0,user_de/0}` may still confuse `installd` on cm-14.1 in a way the wholesale-`rm -rf` *didn't* on May-2 (because the wholesale version most likely never ran at all on May-2 — the `seclabel` directive resolved against a policy that lacked the `lethe` domain, leaving the service in a state that didn't fire).
- **boot.img ramdisk content** differs between May-2 and May-4 builds. Different `default.prop`, different init.rc parsing.

`/proc/last_kmsg` has been unhelpful at every TWRP cycle — TWRP's own boot rewrites the ring buffer, so what we see is bootloader + recovery init messages, not the failed-system-boot tail.

### Decision (2026-05-04, ~17:00) — fork in the road

Rather than continue to grind, three paths are on the table:

A. **Ship the May-2 artifact as v1.0.** Re-promote `Lethe-1.0.0-t0lte.zip.MAY2-BROKEN-NO-SEPOLICY` → `Lethe-1.0.0-t0lte.zip`, re-sign, ship. Trade-off: May-2 likely does *not* actually run burner-wipe (the most plausible explanation for why it boots when May-4 doesn't), so the LinkedIn post's "every reboot wipes user data" line would also need to come out — at which point the v1.0 announcement is hardened-DNS + tracker-blocking + sensor-defaults + no-Google + cosmetics. Honest, unromantic.

B. **One more diagnostic cycle:** TWRP → read `/proc/last_kmsg` (despite the rewrite issue, sometimes a kernel oops *does* survive in `sec_log` or `/sys/fs/pstore`) → 15 min of investigation → if nothing immediate, fall back to A or C.

C. **Stop and ship Monday.** Tag v1.0.0 from a known-good clean lineage tree on a fresh weekday, no overlay accumulation, full validation cycle. Sleep first.

This document records the failure modes encountered today; future work tracks against issue #122 plus a new build-pipeline-hygiene issue for the source-tree drift question.

## Resolution (2026-05-04, ~17:15): ship the May-2 archive

Path A taken. Strip-and-resigned `Lethe-1.0.0-t0lte.zip.MAY2-BROKEN-NO-SEPOLICY` (the artifact from before today's rebuilds) for sideload, Format-Data, sideload — install RC=0, 62 sec. Reboot at 17:13:11. Booted successfully into the LineageOS first-boot setup wizard within a single dexopt cycle.

This is the only build today that boots. It contains:

- LETHE props (`ro.lethe=true`, `ro.build.display.id=LETHE-1.0.0`, all `persist.lethe.*` flags including `burner.enabled=true` and `tor.enabled=true`).
- No compiled LETHE sepolicy (file_contexts.bin has zero LETHE labels).
- LETHE binaries (Tor, lethe-* scripts) **probably** in /system, but the init.lethe-*.rc service files **may not be in the ramdisk** — to be verified once adb is authorized.

The strong working hypothesis for why this artifact boots and the May-4 builds don't: the May-2 zip was probably assembled by re-zipping a vanilla LineageOS OTA with a LETHE-augmented `system/build.prop` and (maybe) a few additional config files, but **without** the LETHE init.rc files in the system image. With no `init.lethe-burner.rc` registered, no `lethe_burner_wipe` service ever runs at post-fs-data, so /data/data is untouched, system_server boots, and the device reaches setup. The May-4 builds shipped the init.rc properly via PRODUCT_COPY_FILES, the `lethe_burner_wipe` service started at post-fs-data, and *something* in the wipe path (or a later interaction) tore /data badly enough that system_server crashed even with the parent-dir-preservation fix.

Net effect on the v1.0 announcement: the *"every reboot wipes user data"* line is in scope only if the device on screen actually wipes on reboot. Pending adb authorization, we'll know within five minutes whether burner runs on this build. If it doesn't, the LinkedIn post needs softening to drop the burner-mode claim alongside the Tor claim. Both, if absent at runtime, become v1.1.

What v1.0 *can* honestly claim with this artifact, regardless:

- `LETHE-1.0.0` build string in About phone — the device IS branded as LETHE.
- No Google services / no GApps (LineageOS-default for unofficial builds).
- Hardened DNS-over-TLS (Quad9 / Mullvad fallback) — set in build.prop, takes effect.
- Tracker-blocking hosts file — shipped at /system/etc/hosts.
- Sensor-permission denials — `persist.lethe.bg_loc=deny`, `persist.lethe.body_sens=deny`, `persist.lethe.near_dev=deny`. These are flags read by LETHE-aware code; without LETHE init services, they're inert. So this is a v1.1 claim too.

The honest v1.0 announcement may end up being closer to: *"a LineageOS-derived build branded LETHE that ships hardened DNS, system-wide tracker blocking, and removes Google services — with the privacy-runtime layer (burner mode, Tor enforcement, sensor lockdown, agent, launcher) deferred to v1.1."* That is materially less exciting than the original draft but is what we can defensibly point at.

### Outstanding for v1.1

1. **Issue #122**: lethe sepolicy crashes system_server on cm-14.1 t0lte. Bisect rule.
2. **Build-pipeline drift between May-2 and May-4**: identify what path produced the May-2 artifact (system contents, init.rc inclusion, ramdisk default.prop) and what changed in `/home/mia/android/lineage` since that made May-4 builds unable to boot even without sepolicy. Suspect: ramdisk regeneration logic in `apply-overlays.sh`, or accumulated patches in `system/sepolicy/` (the `-lethe` exception was one — there may be others).
3. **`scripts/install-sepolicy.sh` cm-12-style target paths**: targets `device/lineage/sepolicy/{,common,vendor}` which don't exist on cm-14.1. The build accidentally worked because `device/samsung/t0lte/selinux/` was populated by an earlier process. v1.0.1 needs to land sepolicy at `device/<vendor>/<codename>/selinux/` deterministically.
4. **`scripts/runtime/lethe-burner-wipe.sh` parent-dir-preservation fix** is in source — must be paired with whatever fix lets it actually run without crashing system_server. Untested on the May-2 ship artifact (because the script likely doesn't run there at all).
5. **OTA artifact provenance**: never reuse a previously-built zip without recording its source-tree SHA + apply-overlays hash. Today's release shipped an unknown-origin artifact because we couldn't reproduce a working build from current sources.

## Post-boot ground truth (2026-05-04, ~17:45)

After authorizing adb, what the May-2 ship build *actually* does on a real Note II:

```
$ getprop ro.build.display.id
lineage_t0lte-userdebug 7.1.2 NJH47F e31c830412 test-keys     # LineageOS-default — not LETHE-1.0.0
$ getprop ro.lethe          → true                              # internal flag — set
$ getprop ro.cm.version     → 14.1-20260502-UNOFFICIAL-t0lte    # LineageOS-style
$ getprop init.svc.lethe_burner_wipe → (empty)                  # service NEVER REGISTERED
$ getprop init.svc.lethe_tor → restarting                       # init: lethe_tor exited 127 (cant exec without sepolicy)
$ getprop init.svc.lethe_settings → stopped                     # oneshot, ran finished
$ getprop init.svc.lethe_mac_rotate → stopped                   # oneshot, registered
```

### Three concrete v1.0 bugs surfaced by this validation, all distinct from issue #122

1. **`ro.build.display.id` in build.prop is set TWICE** — once by LineageOS's buildinfo (line ~5: `lineage_t0lte-userdebug 7.1.2 ...`), once by LETHE's `PRODUCT_PROPERTY_OVERRIDES` (much later: `LETHE-1.0.0`). Android's prop loader takes the **first** value, not the last as we assumed. Result: every UI surface — Settings → About → Build number, About → Phone status — shows `lineage_t0lte-userdebug 7.1.2 ... test-keys`. The device is **visually indistinguishable from vanilla LineageOS** to anyone holding it. About also shows "LineageOS version 14.1-20260502-UNOFFICIAL-t0lte" with no LETHE label anywhere in the UI. Fix in v1.0.1: have apply-overlays.sh either *delete* the LineageOS-default `ro.build.display.id` line before appending the LETHE one, or use `ro.build.display.id := LETHE-1.0.0` (Android.mk override syntax) instead of PRODUCT_PROPERTY_OVERRIDES.

2. **`lethe_burner_wipe` service name is 17 chars; cm-14.1 init's max service name length is 16.** The service definition is silently dropped at parse time; the wipe script never runs. This is why /data/data isn't wiped on every boot — and probably why both the May-2 and the May-4 builds didn't all crash from the wipe (the wipe physically can't fire). The lethe_mac_rotate name is exactly 16 chars and *does* register. Fix in v1.0.1: rename `lethe_burner_wipe` → `lethe_burnwipe` (14 chars) or similar, in both `init.lethe-burner.rc` and any matching `start lethe_burner_wipe` action.

3. **`lethe_tor` service exits status 127.** `127` from a child of init means "command not executable" — not a binary-missing error (the binary IS at `/system/bin/tor`), but a domain-transition refusal under SELinux. Without the lethe sepolicy domain, init's `execute_no_trans` rule on the tor binary's default `system_file` label is denied, the process never starts, and init keeps restarting it on the 5s policy. Same root cause as issue #122. The two services together (lethe_tor + lethe_tor_rules) are how v1.0 was meant to enforce Tor; neither actually runs in this artifact.

### What v1.0 actually delivers, in user terms

Looking at the device on a desk:
- It boots. Setup wizard works. Trebuchet home screen reachable.
- About screen says **LineageOS** in three places, never LETHE.
- No SIM signal (test device). Wi-Fi available.
- No Google services (LineageOS without GApps).
- The privacy-runtime layer this OS was supposed to enforce (Tor, burner, agent) doesn't run.

What's defensibly true and could be in the post:
- Build is debloated and Google-services-free (LineageOS-default for unofficial builds, but it ships in our packaged OTA).
- DNS-over-TLS is configured to Quad9 / Mullvad (props are set; Android resolver reads them).
- `/system/etc/hosts` ships a tracker block list (LineageOS-default + LETHE additions).
- `persist.lethe.{bg_loc,body_sens,near_dev}=deny` are set (but are runtime hints to LETHE-aware code that doesn't exist yet — so functionally invisible).

### Recommended call

Given that the user-visible state of the v1.0 ship artifact is "vanilla LineageOS with internal `ro.lethe=true`", and the differentiating runtime layer (burner / Tor / agent) doesn't actually fire, the honest options are:

- **Defer to Monday.** Fix issues 1 and 2 (display.id ordering + service name length), do a clean rebuild from a clean tree, validate the boot path end-to-end including burner running, then ship. Tor / sepolicy stays in v1.0.1.
- **Ship today as a research preview** with a LinkedIn post that's *very* explicit: "v1.0 is a build artifact + manifest + scripts; the runtime privacy layer is v1.1." Risk: the post is harder to write honestly, and the device under any tester's eye is just LineageOS.

Bug 2 (16-char service name) is small enough to fix and rebuild today *if* the rebuild also somehow avoids the unknown source-tree drift bug that crashed all today's May-4 builds. Given we couldn't isolate that drift in 6 hours, deferring is more honest.

## Final state of today's session (2026-05-04, ~19:00)

After surfacing the four release-blocking bugs (#122-#125), we fixed what was source-fixable today:

- **#125**: `lethe_burner_wipe` → `lethe_burnwipe` (≤16 chars) in [initrc/init.lethe-burner.rc](../../initrc/init.lethe-burner.rc).
- **#124**: split LETHE identity props between `PRODUCT_DEFAULT_PROPERTY_OVERRIDES` (display-id, locks via /default.prop / read-only-prop semantics) and `PRODUCT_PROPERTY_OVERRIDES` (LETHE-namespace) in [apply-overlays.sh](../../apply-overlays.sh). Verified via boot.img ramdisk extraction — new `default.prop` has `ro.build.display.id=LETHE-1.0.0`.
- **+1**: discovered and renamed `persist.lethe.bfu.timeout_minutes` (33 chars) → `persist.lethe.bfu.timeout` (25 chars) — same family of bug as build-notes #2 (PROPERTY_KEY_MAX is 31). Updated across `overlays/privacy-defaults.conf`, the Java `BfuRebootService.java` + `LetheConfig.java`, and the JS `settings-bfu.js` + `settings-border-mode.js`.
- **+1**: versioned the LETHE-identity idempotency marker in apply-overlays.sh (`v3` after the bfu rename); old marker → strip-and-rewrite the block on next run. So future apply-overlays.sh re-runs against an already-patched tree pick up source changes.

Rebuilt cleanly with all four fixes in place at 18:11. Sideloaded RC=0 first attempt at 18:15:38. **The device still crash-looped** for 40+ minutes and dropped off USB.

**This proves #123 isn't downstream of #122/#124/#125** — it's an independent regression in the local lineage tree that we cannot isolate from inside this session. The fixes are correct (and verifiable in the artifact), but they can't be runtime-validated until v1.0.1 is built from a fresh `repo init` clone.

### Action items for v1.0.1 / next session

1. **Clean tree** — `repo init -u https://github.com/LineageOS/android.git -b cm-14.1; repo sync` in a fresh dir. Apply LETHE patches via apply-overlays.sh on the clean tree. Build.
2. **If clean tree works**: ship as v1.0.1. Cleanup commits and tag.
3. **If clean tree also fails**: now we know the regression is in our patches, not the local tree. Bisect against the May-2 known-good source-tree state (will need to identify what produced that artifact first — see #123 mitigation suggestions).
4. **Issues #122 (sepolicy crash)** stays for v1.1 — needs rule-by-rule bisection of `sepolicy/lethe.te`. The `lethe.te.disabled-in-v1.0` and `file_contexts.disabled-in-v1.0` files in the LETHE repo carry the policy forward for that work.

Today's commits surface bugs and fix two; the release ships when fixes can be runtime-validated.

## Last test of the night (2026-05-04, 20:13) — drift is in source, not `out/`

Clobbered `/home/mia/android/lineage/out/` entirely (preserving ccache + source) and rebuilt cold. 29:29 wall time, all four fixes correctly baked into the artifact (verified: ramdisk default.prop has `LETHE-1.0.0`, init.rc has `lethe_burnwipe`, `persist.lethe.bfu.timeout` fits, no sepolicy in file_contexts.bin). Sideloaded RC=0 (attempt 2 after one SIGBUS). Reboot at 19:32:42.

**Same crash-loop outcome.** 40 min later the Note II was off USB again, identical to every previous May-4 build. Clobbering `out/` did not change the failure mode.

This proves the regression is **in the local source tree**, not in stale intermediates. Something patched into `/home/mia/android/lineage` between when the May-2 archive was produced and when today's session started crashes system_server, and we have not isolated which file is the culprit. The fix surface is exhausted from inside this tree.

### Hard gate before any v1.0.1 attempt

`repo init -u https://github.com/LineageOS/android.git -b cm-14.1` in a fresh directory. `repo sync` (multi-hour download). Apply LETHE patches via apply-overlays.sh on the fresh tree. Build. If that boots: cleanup commits, ship as v1.0.1, file an issue covering the corrupted tree at `/home/mia/android/lineage` so it can either be repaired or abandoned. If the fresh tree *also* fails: the regression is in our patches, not the local tree, and bisect against the May-2 known-good source-tree state — first identifying what produced that artifact (see issue #123).

### Today's actual deliverable

Four release-blocking bugs surfaced and filed (issues #122–#125). Two source fixes (#124, #125) plus a bonus prop-key-length fix (`bfu.timeout_minutes` → `bfu.timeout`) plus apply-overlays.sh idempotency hardened to versioned markers. The investigation log itself, plus screenshots of the May-2 ship-build's actual user-facing state. None of it is runtime-validated tonight; all of it goes into the v1.0.1 attempt against a fresh tree.

Stopping at 20:13 after 12+ hours. v1.0 does not ship today.

## 2026-05-08 → -09 overnight — fresh-tree rebuild eliminated all software hypotheses

Followed the hard-gate plan: `repo init -u https://github.com/LineageOS/android.git -b cm-14.1` in `~/android/lineage-v1.0.1`, applied LETHE overlays, built clean. The build itself succeeded; subsequent boot tests did not. Recap:

### Build pipeline — green

`mka bacon` inside `Dockerfile.cm14-build` from the freshly-synced tree completed in **33:42 wall** (warm ccache helps; subsequent rebuilds expected at 5–10 min). Required workarounds beyond the documented flow:

- **Local manifest** copied across (`.repo/local_manifests/t0lte.xml`); cm-14.1's default manifest doesn't include the Note II device tree any more.
- **vendor/samsung blobs** are not in any public repo; copied verbatim from `~/android/lineage/vendor/samsung/` (15 MB, never re-extractable since `extract-files.sh` requires the device booted to system).
- **Hand-edit on `build/target/product/core_minimal.mk`** — `webview` line removed from `PRODUCT_PACKAGES`. Lives only in the working tree of `~/android/lineage/`, **not** in `apply-overlays.sh`. Without mirroring this onto the fresh tree the build fails on the missing chromium-webview LFS prebuilts. Either fetch the LFS blobs going forward or fold the edit into `apply-overlays.sh` so v1.0.x rebuilds are reproducible.
- **Docker env**: `Dockerfile.cm14-build` sets `LANG=LC_ALL=en_US.UTF-8`, but cm-14.1's `flex-2.5.39` prebuilt aborts on modern glibc with `_nl_intern_locale_data` assert under any non-`C` locale. Build container needs `LC_ALL=C LANG=C`. Also needs `USER=root HOME=/root` (Jack server crashes with `unbound variable: USER`) and `git -c safe.directory='*'` to dodge fresh-clone ownership warnings on `.repo/manifests`. None are baked into `Dockerfile.cm14-build` yet — currently passed via `docker run -e ...` in the ad-hoc invocation.

Build artifact preserved as `~/Osmosis-downloads/lethe-builds/Lethe-1.0.0-t0lte.zip.MAY8-FRESH-TREE-CRASH-LOOPS` (273,720,222 B, sha256 `8fcf1fc5…`). Companion `.build.log` next to it.

### Sideload pipeline — broken in two places, fixed in two commits

Running `make sideload-noassert` against the fresh build surfaced two latent host-side bugs:

1. **`scripts/lethe-resign.sh`** invokes `signapk.jar` without `-Djava.library.path`. Inside the build, `mka bacon` sets the path; outside, signapk dies on `UnsatisfiedLinkError: no conscrypt_openjdk_jni`. Fix in **`09d90c6`**: default `SIGNAPK_NATIVE_DIR` to `$LINEAGE_TREE/prebuilts/sdk/tools/linux/lib64`, pass via `-Djava.library.path`.
2. **`/usr/bin/zip` (Info-ZIP) silently fails when invoked from a Make recipe.** `.ONESHELL + SHELLFLAGS=-euo pipefail -c` produces a context where `zip -r ARCHIVE .` emits `zip warning: name not matched: <archive>` then `updating:` lines for every file, exits rc=0, but the archive doesn't exist on disk afterwards. Reproduces with positional args, `zip -@` stdin lists, absolute and relative archive paths. Same script via `bash -euo pipefail -c` directly works fine. Root cause unidentified; sidestepped in **`adced25`** by replacing the repack step with Python's `zipfile` module (cm-14.1 build container's stdlib has it). Mention this to anyone reaching into `lethe-resign.sh` later — `/usr/bin/zip` is poison there.

### Boot-test cascade — every software variable disproven

Five fresh-boot-tests on the same Note II t0lte device, all fail with the same symptom: device sits on the LineageOS bootanim, `wait-boot` times out at 45 min, device drops off USB.

| # | What was different | Result |
|---|---|---|
| 1 | LETHE fresh-tree build, defaults (burner=true) | bootanim, kmsg shows zygote SIGKILL at uptime 2794s — userspace crash after dexopt |
| 2 | `persist.lethe.burner.enabled=false` seeded on freshly-formatted /data | bootanim, no kmsg (power cycle wiped ramoops) |
| 3 | All `init.lethe-*.rc` (`burner`, `tor`, `settings`) moved to `/system/etc/init/_lethe_disabled/` | bootanim |
| 4 | Vanilla cm-14.1 (`Vanilla-cm14-t0lte.zip`, 271 MB May-2 known-good — booted on this device on May 2 first-boot wizard) | bootanim |
| 5 | Vanilla cm-14.1 with `WIPE_SYSTEM=1 WIPE_CACHE=1` — full partition format, no block-OTA patching of stale data | bootanim |

Each crosses out a hypothesis class:
- (1)→(2) burner-wipe of /data is not the cause
- (2)→(3) other LETHE init.rc services aren't either
- (3)→(4) **no LETHE artifact at any layer is the cause** (vanilla also fails on the same hardware that booted vanilla a week earlier)
- (4)→(5) block-level corruption in /system carried across installs is not the cause (formatted /system also fails)

This eliminates everything checkable from the host side. The Note II device itself is the blocker.

### Hardware path before any more software iteration

1. **Wall charger**, not a PC USB-2 port (~0.5 A is below peak draw under dexopt; brown-out is the most plausible mode given ~3.5 hours of high-CPU load across today's runs and a years-old battery).
2. **Odin/Heimdall full-image flash of stock Samsung firmware** — bypasses TWRP + block-OTA entirely, writes raw partitions including the bootloader and modem regions that no test today touched. If after that vanilla cm-14.1 still loops on bootanim, the eMMC is failing or the device is otherwise unfit for v1.0.1 testing.
3. **Battery swap** (Note II batteries are user-replaceable; new cells are still cheap and plentiful) before assuming the device is dead.
4. **Alternate t0lte handset** as the eventual fallback — the v1.0.x test fixture should not be a single 13-year-old phone.

If hardware comes back and a vanilla flash boots cleanly, **resume** the LETHE bisect: revert May-4 build-baked commits (`17b84ce`, `5d4d29d`, `c7cad54`, `621e417`) one at a time on top of `~/android/lineage-v1.0.1`, rebuild (5–10 min via warm ccache), `fresh-boot-test ZIP=… WIPE_SYSTEM=1`, until something boots. The non-LETHE-init.rc-resident half of the regression hypothesis space is still untested — only ruled in by elimination, not by data.

### Tooling added during the diagnosis (worth keeping regardless)

`scripts/Makefile` gained the runtime-hypothesis-test rig:

- `persist-set KEY=… VAL=…` / `persist-clear KEY=…` / `persist-list` — write/clear/list `/data/property/persist.<key>` overrides from TWRP. Init reads these at post-fs and they override `build.prop` defaults until cleared or wiped. Lets us flip a single property without rebuilding.
- `wipe-system` / `wipe-cache` — the standalone wipes that `wipe-data` already had.
- `fresh-boot-test ZIP=… [WIPE_SYSTEM=1] [WIPE_CACHE=1] [PERSIST_KEY=… PERSIST_VAL=…]` — full clean-room cycle (wipe → resign+sideload → optional persist seed on freshly-formatted /data → reboot → wait-boot). Each phase prints UTC `HH:MMZ` plus a worst-case ETA so a 50-minute run is readable on scrollback.

Five commits land locally (not pushed): `09d90c6`, `6b8e2ff`, `5fc9b46`, `adced25`, `45f059b`. None are tagged; v1.0.1 still waits on a green boot.

### Standing state for the next attempt

- `~/android/lineage-v1.0.1/` — clean cm-14.1 source tree with t0lte local manifest, vendor blobs, and overlays applied. 45 GB on disk. `repo sync` is **not** needed again; ccache is primed (8.5 GB).
- `~/Osmosis-downloads/lethe-builds/Lethe-1.0.0-t0lte.zip.MAY8-FRESH-TREE-CRASH-LOOPS` plus its `.build.log` — preserved as evidence; do not overwrite until v1.0.1 ships.
- `~/Osmosis-downloads/lethe-builds/Lethe-1.0.0-t0lte.zip` (the May-4 dirty-tree golden, sha256 `b616054f…`) is still in place under that name; v1.0.0 git tag (`540d08e`) is unchanged on origin.
- 5 commits on local main past `5248156` (the last commit on origin/main as of 2026-05-08 morning). Push and tag only after a green boot.
